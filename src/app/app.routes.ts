import { Routes } from '@angular/router';
import { AppComponent } from './app.component'; // Importe seus componentes aqui

export const routes: Routes = [
  { path: '', component: AppComponent }, // Exemplo de rota
  // Adicione outras rotas aqui
];

